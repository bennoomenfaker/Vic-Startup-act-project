services:
  - type: web
    name: vic-startup-act-analytics
    runtime: node
    buildCommand: npm install && npm run build
    startCommand: node server.cjs
    healthCheckPath: /
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 3001
